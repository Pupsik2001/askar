import { Box } from '@chakra-ui/react'
import styled from '@emotion/styled'

export const NumSection = styled(Box)`
  padding-left: 3.4em;
  text-indent: -3.4em;
`

export const NumYear = styled.span`
  font-weight: bold;
  text-align: center;
  display: block;
  margin: 0 auto;
  line-height: 1.2;
`
